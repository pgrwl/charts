---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "pgrwl.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pgrwl.labels" . | nindent 4 }}
spec:
  replicas: 1
  serviceName: {{ include "pgrwl.fullname" . }}
  selector:
    matchLabels:
      {{- include "pgrwl.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "pgrwl.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "pgrwl.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: {{ include "pgrwl.fullname" . }}
            readOnly: false
        - name: pgrwl-config
          configMap:
            name: {{ include "pgrwl.fullname" . }}
      initContainers:
        - name: wait-db
          image: "busybox:1.37.0"
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -euc
            - |
              while ! nc -z "${PGHOST}" "${PGPORT}"; do
                echo "Waiting for PostgreSQL to boot";
                sleep 5;
              done;
              echo "PostgreSQL is ready";
          envFrom:
            - secretRef:
                {{- if .Values.pgconn.existingSecret.enabled }}
                name: {{ .Values.pgconn.existingSecret.name }}
                {{- else }}
                name: {{ include "pgrwl.fullname" . }}
                {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.args }}
          args: {{- include "pgrwl.tplvalues.render" (dict "value" .Values.args "context" $) | nindent 12 }}
          {{- end }}
          env:
          {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
          {{- end }}
          envFrom:
            - secretRef:
                {{- if .Values.pgconn.existingSecret.enabled }}
                name: {{ .Values.pgconn.existingSecret.name }}
                {{- else }}
                name: {{ include "pgrwl.fullname" . }}
                {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - mountPath: /wals
              name: storage
            - mountPath: /etc/pgrwl
              name: pgrwl-config
