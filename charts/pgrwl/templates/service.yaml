---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgrwl.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pgrwl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgrwl.selectorLabels" . | nindent 4 }}
